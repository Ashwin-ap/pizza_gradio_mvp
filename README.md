# SliceMatic — Stage 2 MVP

A Gradio-based pizza ordering wizard.

## Requirements

- Python 3.10–3.12
- Gradio pinned at `5.49.1` (do **not** upgrade — bare install pulls breaking 6.x)

## Run

```bash
pip install -r requirements.txt
python app.py
```

## Test

```bash
pytest -q
```
